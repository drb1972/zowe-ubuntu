Z Developer Portal - GCP
Create UBUNTU
10.252.108.65
VSCode Remote Explorer - Remote Explorer
ssh root@10.252.108.65 -A
root - Debug411

#Definir nuevo usuario
adduser dr891415
usermod -aG sudo dr891415
su dr891415
cd ~
#No hace falta 
sudo apt-get -y install git-all
git clone https://github.com/drb1972/zowe-ubuntu.git
cd zowe-ubuntu
chmod +x init.sh
./init.sh

INCLUIDO
========
#Install node.js & npm
sudo apt-get -y install nodejs
sudo apt-get -y install npm
sudo npm install -g n
sudo npm install -g prebuild-install
sudo apt-get install -y gnome-keyring
sudo apt-get install -y libsecret-1-0
export $(dbus-launch)
sudo export $(dbus-launch)
sudo gnome-keyring-daemon --unlock --components=secrets #Pulsar Control-D 
sudo apt-get install -y libpam-gnome-keyring

NO INCLUIDO
===========
#etc/pam.s/sshd  
#incluir al final: auth optional pam_gnome_keyring.so
#incluir al final de la seccion session: session optional pam_gnome_keyring.so auto_start
#Incluir en ~/.bashrc. 
   if test -z "$DBUS_SESSION_BUS_ADDRESS" ; then
   exec dbus-run-session -- $SHELL
   fi
   gnome-keyring-daemon --start --components=secrets

INCLUIDO
========
#db2 odbc
sudo mkdir nodeapp
cd nodeapp
sudo npm install ibm_db
sudo npm install async bluebird moment
cd ..

INCLUIDO
========
#Install Zowe
sudo npm install -g @zowe/cli@zowe-v2-lts o sudo npm install -g zowe-cli.tgz

zowe plugins install @zowe/cics-for-zowe-cli@zowe-v2-lts @zowe/db2-for-zowe-cli@zowe-v2-lts @zowe/ims-for-zowe-cli@zowe-v2-lts @zowe/mq-for-zowe-cli@zowe-v2-lts @zowe/zos-ftp-for-zowe-cli@zowe-v2-lts
zowe plugins install @zowe/secure-credential-store-for-zowe-cli

zowe plugins install ca7-for-zowe-cli.tgz
zowe plugins install endevor-for-zowe-cli.tgz
zowe plugins install mat-detect-for-zowe-cli.tgz
zowe plugins install caview-for-zowe-cli.tgz
zowe plugins install file-master-plus-for-zowe-cli.tgz
zowe plugins install microfocus-zmf-cli-plugin-1.0.3.tgz
zowe plugins install ops-for-zowe-cli.tgz
zowe plugins install endevor-bridge-for-git-for-zowe-cli.tgz
zowe plugins install mat-analyze-for-zowe-cli.tgz
zowe plugins install om-spool-for-zowe-cli.tgz



#Install ooRexx 
cd zowe-ubuntu 
sudo dpkg -i ooRexx-5.0.0-12528.ubuntu1604.x86_64.deb

--------

File - Preferences - Keyboard shortcuts - json
// Place your key bindings in this file to override the defaults
[
    {
       "key": "ctrl+enter",
       "command": "workbench.action.terminal.runSelectedText",
       "when": "editorTextFocus&& editorHasSelection"
    }
]

------
#Instalar extensiones
code --install-extension ms-vscode.remote-explorer-0.1.2022103109.vsix
code --install-extension broadcomMFD.code4z-extension-pack-2.0.1.vsix
code --install-extension broadcomMFD.lsp-for-rexx-0.0.18.vsix 
code --install-extension broadcomMFD.data-editor-for-mainframe-1.0.0.vsix
code --install-extension broadcomMFD.bridge-for-git-explorer-0.3.0.vsix

---------

Virtual box
sudo apt get git
sudo apt-get install virtualbox-guest-additions-iso
#ejecutar desde el CD autorun right click
